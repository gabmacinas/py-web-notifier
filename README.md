# TestFlight slot checker for iOS

## tool used for checking beta status and it will only notify users when slot is available. currently works using macos

- Installation
  - to install
  ```bash
    pip3 install selenium
  ```
  - then run
  ```bash
    python init.py
  ```
  